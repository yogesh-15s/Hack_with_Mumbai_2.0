{
    "name": "medvault-server",
    "version": "1.0.0",
    "description": "Backend for MedVault",
    "main": "src/index.js",
    "scripts": {
        "start": "node src/index.js",
        "dev": "nodemon src/index.js",
        "build": "cd ../client && npm install && npm run build && cd ../server && npx --package=prisma@5.22.0 prisma generate && npx --package=prisma@5.22.0 prisma migrate deploy"
    },
    "dependencies": {
        "@prisma/client": "5.22.0",
        "prisma": "5.22.0",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "jsonwebtoken": "^9.0.2",
        "multer": "^2.0.2"
    },
    "devDependencies": {
        "nodemon": "^3.0.3"
    }
}